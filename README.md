# AI Dashboard Generator (Streamlit + React)

## Streamlit Cloud Deployment
1. Upload this repo to GitHub
2. Set `OPENAI_API_KEY` in Streamlit Secrets
3. Set main file as `app.py`
4. Deploy

No Docker required.